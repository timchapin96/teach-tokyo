
  <div class="new-game d-none" data-new_game-target = "newGame">
    <%= simple_form_for @game do |f| %>
      <%= f.input :title %>
      <%= f.button :submit, "Submit", class: "btn btn-success" %>
    <% end %>
  </div>
