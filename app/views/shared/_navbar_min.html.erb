<% if user_signed_in? %>
  <div class="menu" data-controller = "menu">
    <%= image_tag "menu-bars.svg", id: "menu-bar", data: {action: "click->menu#menuPopup"} %>
    <div class="popup-menu">
      <%= link_to "Home", root_path %>
      <p data-controller = "new_game menu" data-action = "click->new_game#displayForm click->menu#menuPopup">New</p>
      <p id = "load-button" data-controller="load menu" data-action="click->load#load click->menu#menuPopup">Load</p>
      <p><%= link_to "Games", games_path %></p>
      <p id = "options-button" data-controller="options" data-action="click->options#optionsLoad">Options</p>
    </div>
  </div>
<% end %>
<nav class= "navbar-min">
  <div class="nav-elements">
    <% if user_signed_in? %>
      <div class="profile">
        <%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete} %>
        <%= image_tag "default_user.png", class: "avatar" %>
      </div>
    <% else %>
      <div class="profile">
        <%= link_to "Login", new_user_session_path %>
      </div>
    <% end %>
  </div>
</nav>
